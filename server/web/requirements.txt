# Pinned versions avoid pip backtracking (very slow Docker builds when using wide >= ranges).
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.1
pydantic==2.10.6
redis==5.0.1
aiofiles>=24.1.0,<27
websockets>=13.1,<16.0
jinja2==3.1.3
psutil==5.9.8
python-multipart==0.0.6
anthropic==0.100.0
openai==2.36.0
# Starlette TestClient (FastAPI tests) requires httpx<0.28 — 0.28 removed `app=` on Client.
httpx==0.27.2
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.1.2
google-auth==2.38.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.167.0
fpdf2==2.8.2
alembic==1.15.2
sqlalchemy==2.0.38
slowapi==0.1.9
mem0ai==2.0.2
# Self-hosted Mem0 backends — must be installed explicitly (not pulled by bare mem0ai==2.0.2).
# Mirrors the packages in the official mem0 Docker image (blog.mem0.ai/self-host-mem0-docker).
psycopg[binary,pool]>=3.1,<4  # pgvector async connection pool
psycopg2-binary>=2.9,<3       # psycopg2 fallback (mem0 pgvector provider uses this internally)
neo4j>=5.14,<6                # Neo4j Bolt driver
langchain-neo4j>=0.2,<1       # Neo4j graph store wrapper used by mem0 graph provider
rank-bm25>=0.2.2               # BM25 hybrid search scorer
apscheduler>=3.10,<4           # background analysis jobs (daily digest, post-meeting synthesis)
# Pipecat AI voice pipeline: STT (OpenAI Realtime) + LLM (GPT-4o) + TTS (OpenAI).
# Requires websockets>=13.1 and aiofiles>=24.1.0.
pipecat-ai[openai]==1.2.1
# Email attachment text extraction (PDF, Word, Excel, PowerPoint).
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
