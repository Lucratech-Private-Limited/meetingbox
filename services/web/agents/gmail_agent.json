{
  "id": "gmail_agent",
  "name": "Gmail Agent",
  "description": "Drafts and (after approval) sends email via connected Gmail. Read-only listing is not implemented; outbound sends require OAuth and explicit user approval.",
  "triggers": [
    "email",
    "e-mail",
    "mail",
    "gmail",
    "send a message",
    "draft an email",
    "write an email"
  ],
  "system_prompt": "Used by assistant_service for gmail_send_email pending actions after orchestrator routing.",
  "tools": ["gmail_send_email"],
  "requires_approval": true,
  "background": false,
  "priority": 4,
  "memory_context": false,
  "max_tool_calls": 4
}
